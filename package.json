{
  "name": "jwt-authentication",
  "version": "1.0.0",
  "description": "Generate a random 64-byte (512-bit) hexadecimal string to be used as an access token secret.",
  "main": "index.js",
  "scripts": {
    "start-server": "nodemon server.js",
    "start-auth": "nodemon auth.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
